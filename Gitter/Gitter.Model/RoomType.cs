namespace Gitter.Model
{
    public enum RoomType
    {
        Organization,
        Repository,
        OneToOne,
        OrganizationChannel,
        RepositoryChannel,
        UserChannel
    }
}
